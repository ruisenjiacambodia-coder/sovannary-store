[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./sovannary"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
healthcheckPath = "/"
healthcheckTimeout = 100